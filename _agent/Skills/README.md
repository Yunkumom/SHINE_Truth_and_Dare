# Project Skills / 專案 Skills

No active project-specific Skills have been created.

目前尚未建立作用中的專案專用 Skill。

Create a Skill package directly under this directory only when a reusable capability is confirmed. Every package must contain `SKILL.md`; do not create empty support folders.

只有確認需要可重用能力時，才直接在此目錄建立 Skill package。每個 package 必須包含 `SKILL.md`，不得建立空白支援資料夾。

