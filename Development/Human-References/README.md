# Human References / 人類參考資料

The primary desktop entry point is the root `Open Truth and Dare.cmd`, which opens `Apps/Standalone/encounter_cards_v16.html` through the required loopback HTTP origin. Files here are supporting human-facing references, not product runtime files.

主要電腦入口為根目錄 `Open Truth and Dare.cmd`，它會透過必要的 loopback HTTP origin 開啟 `Apps/Standalone/encounter_cards_v16.html`。本區檔案是人類可讀支援參考，不是產品 runtime。

- `system-map.html`: current product/source/build flow.
- `quick-fixes.html`: common maintenance locations and checks.
- `dashboards/`: retained standalone visual references.
