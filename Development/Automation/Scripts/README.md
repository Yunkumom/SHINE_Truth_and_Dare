# Scripts / Node 自動化

Deterministic Node scripts for card extraction, PWA finalization, standalone export, and public-release finalization. Run them only through documented product commands.

保存卡牌抽取、PWA finalization、standalone 輸出與公開版 finalization 的可重現 Node scripts。只能透過文件化流程執行。
