# Main App v28 Source / 主應用程式 v28 原始碼

This is the React, TypeScript, and Vite source for Encounter Cards v28. It preserves the complete v27 experience and introduces a wider, portrait-safe Baosheng Dadi apothecary artwork whose complete Taiwan enamel is calibrated to the long-press coastline reveal.

此處保存 Encounter Cards v28 source；完整保留 v27 體驗，並加入構圖更寬、頭冠安全、台灣圖案完整且與長按海岸線定位校準的保生大帝藥舖圖。

Restore dependencies with `npm ci`, then use `npm run typecheck`, `npm run lint`, `npm test`, and `npm run build`. Verified v28 outputs become immutable; later behavior must use the next numbered version.
