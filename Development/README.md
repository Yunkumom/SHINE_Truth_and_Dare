# Development / 開發資料

Everything used to design, build, test, document, govern, or troubleshoot the products belongs here. Finished release outputs belong in `../Apps/`; reusable source assets belong in `../Assets/`.

所有設計、建置、測試、文件、治理與除錯內容都放在此處。完成的 release 放入 `../Apps/`；可重用原始素材放入 `../Assets/`。
