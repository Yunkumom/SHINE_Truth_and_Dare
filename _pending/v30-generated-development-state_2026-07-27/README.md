# v30 generated development state / v30 生成開發狀態

This folder preserves reproducible local-only v30 dependencies and TypeScript build caches moved out of active Development after all source tests, typecheck, lint and release builds passed. These files are not authored source or publication artifacts. Restore future dependencies with `npm ci`.

本資料夾保留 v30 通過測試、型別、lint 與 release 建置後移出的可重建本機依賴與 TypeScript cache；這些不是人工 source 或發布成品，後續請用 `npm ci` 還原依賴。
