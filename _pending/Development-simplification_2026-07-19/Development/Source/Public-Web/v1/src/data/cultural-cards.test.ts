import{describe,it,expect}from'vitest';import{culturalCards}from'./cultural-cards';describe('cultural cards',()=>{it('contains six sourced cards per region',()=>{expect(culturalCards.filter(c=>c.region==='Taiwan')).toHaveLength(6);expect(culturalCards.filter(c=>c.region==='Australia')).toHaveLength(6);expect(new Set(culturalCards.map(c=>c.id)).size).toBe(12);for(const c of culturalCards){expect(c.sourceUrl).toMatch(/^https:\/\//);expect(c.attribution).toBeTruthy();expect(c.visualRestrictions).toContain('no sacred')}})})
