import React from'react';import{describe,it,expect}from'vitest';import{createRoot}from'react-dom/client';import{act}from'react';import App from'./App';describe('original card contract',()=>{it('keeps the v16 hierarchy',()=>{const host=document.createElement('div');act(()=>createRoot(host).render(<App/>));const card=host.querySelector('.card-stack .draw-motion .encounter-card .card-face.card-front .mythic-card');expect(card?.querySelector('.mythic-card-header')).toBeTruthy();expect(card?.querySelector('.mythic-art-frame')).toBeTruthy();expect(card?.querySelector('.mythic-text-panel')).toBeTruthy();expect(host.querySelectorAll('.stack-card')).toHaveLength(2)})})
