# Tools / PowerShell 工具

PowerShell helpers for deterministic v16 generation and loopback-only local serving. Do not expose the server beyond `127.0.0.1`.

保存 v16 可重現建置與僅限 loopback 的本機執行工具。Server 不得開放到 `127.0.0.1` 以外。
