# Tests / 驗證

Repository and release contract validators live here. Run `validate_repository.ps1` before claiming completion; it invokes focused release checks.

此處保存 repository 與 release 合約驗證。宣告完成前必須執行 `validate_repository.ps1`；它會呼叫各版本專用檢查。
