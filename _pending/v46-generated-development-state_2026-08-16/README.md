# v46 generated development state — 2026-08-16

- This folder preserves the recoverable `node_modules/`, `.sites-runtime/`, `.vinext/`, and `.wrangler/` state produced by the successful v46 build and tests.
- These files are local build evidence, not authored product source. Restore dependencies with `npm ci`; future builds recreate the remaining caches and deployment scaffolding.

- 本資料夾保留 v46 成功建置與測試後產生、可回復的 `node_modules/`、`.sites-runtime/`、`.vinext/` 與 `.wrangler/` 狀態。
- 這些檔案是本機建置證據，不是人工維護的產品 source。依賴請用 `npm ci` 還原；其餘 cache 與部署 scaffolding 會由後續建置重新產生。
