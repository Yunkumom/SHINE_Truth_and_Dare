# v36 Generated Development State

Recoverable `node_modules/` and TypeScript build-info caches moved out of active `Development/Source/Main-App-v36/` after the verified v36 tests and release build. These are generated files; restore dependencies with Node 26 and `npm ci` when creating the next versioned product line.

已完成 v36 測試與 release 建置後，將可回復的 `node_modules/` 與 TypeScript build cache 移出作用中 Development。這些均為生成檔；建立下一個版本時使用 Node 26 與 `npm ci` 還原。
