# v25 generated development state

This recoverable holding area contains reproducible `node_modules/` and TypeScript build caches removed from active Development after v25 verification. They may be replaced with `npm ci` and a fresh build from `Development/Source/Main-App-v25/`.

本區保存 v25 驗證後移出作用中 Development 的可重建依賴與 TypeScript cache；可由 `npm ci` 與重新建置取代。
