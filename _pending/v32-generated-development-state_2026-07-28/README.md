# v32 generated development state

Recoverable v32 `node_modules` and TypeScript build caches moved out of active Development after the verified v32 build. Restore dependencies with `npm ci`; these files are not authored source or release output.

此處保存 v32 驗證後移出的可重建依賴與 TypeScript cache；它們不是人工 source 或 release 成品，需要時可用 `npm ci` 重建。
