# v29 Pre-verification Build

This recoverable checkpoint contains the first v29 standalone and Public Web v12 outputs generated before browser QA found that the legacy English blessing selector overrode the new adjustable font scale. It is evidence only and must not be published.

此可回復檢查點保存瀏覽器驗證前的第一版 v29 成品。該版本的英文祝福仍被舊 CSS 字級覆寫，因此只能作為證據，不可發布。
