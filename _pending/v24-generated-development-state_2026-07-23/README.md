# v24 generated development state

This recoverable archive contains the reproducible `node_modules/` directory and TypeScript build caches moved out of active `Development/Source/Main-App-v24/` after final source tests and release generation. Restore dependencies normally with `npm ci`; do not treat these files as authored source.

此可回復封存保存 v24 最終測試與發行後移出作用中 Development 的 `node_modules/` 與 TypeScript build cache。一般應使用 `npm ci` 重建，不應視為人工 source。
